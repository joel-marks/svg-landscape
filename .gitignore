# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build output
dist
dist-ssr
*.local

# Claude Code's per-machine permission file. The project's standing permissions
# (CLAUDE.md, Operating scope) live in the tracked .claude/settings.json as
# broad Bash/Edit/Write rules; this file only ever accumulated auto-recorded
# one-off entries, one of which needed its own commit in Phase 7.5.
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
